extends ../base.pug
include ../mixins/video

block content 
    div.profile-data
        img(src="/" + user.avatarUrl).avatar
        h3=user.username
    div.video_grid
        each potato in user.video
            +video(potato)
        else 
            span.empty-message Not found Video